import pandas as pd

def cube_Categorize(samples, d):
    # to which bin does a cube belong? depends on the selected features
    # cat_arr = []
    # for i in range(len(samples)):
    #     samp = samples.iloc[i,:]
    #     array_mu_x = samp[0:12]
    #     array_mu_y = samp[12:24]
    #     array_mu_z = samp[24:36]
    #     # print(array_mu_x)
    #     # print(array_mu_x.max())
    #     # print(array_mu_y)
    #     # print(array_mu_y.max())
    #     # print(array_mu_z)
    #     # print(array_mu_z.max())
    #     cat_arr.append([array_mu_x.max(), array_mu_y.max()])
    # df_cat = pd.DataFrame(data=cat_arr)
    # # print("df_cat")
    # # print(df_cat)
    # return df_cat
    return samples
    
